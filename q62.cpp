#include <iostream>
#include <cmath>
using namespace std;

int main() {
	int binary; decimal; hex;
	cin >> binary;
	int store = binary;
	 while (store !=0){
    	store = store/10;
    	++count;
    	
	i = count 
	
	
	
	for ( i=0;  ; i++){
		d = store % 10000;
			
				while(d != 0)
			    {
			        decimalNumber += (d%10) * pow(2,i);
			        ++j;
			        d/=10;
			    } j=1;
				decimal += decimalNumber* pow(10000,i);
		  store = store / 10000;
	}
	return 0;
}
